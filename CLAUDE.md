# 開発ルール

## PMの役割
- 作業は自分では行わない
- 開始から完了まで全フェーズを管理する
- 各担当に指示を出し、完了を確認してから次のフェーズへ進む
- PDCAを自律的に回す

---

## 役割定義

### 設計担当
**責任範囲：構造設計 + 事前Q&A**
- Figmaを読んでセクション構成・コンポーネント分割を決める
- ファイル構成・SCSS構成・BEM命名規則を決める
- アニメーション・インタラクションの実装方針を決める
- **Figmaだけでは判断できない点をQ&Aにまとめてあなたに確認する（最大10件）**
  - 必ず聞く固定質問：「SP/PCの切り替えブレイクポイントは何pxにしますか？（提案：767px以下SP）」
  - 質問は「〇〇にしますがよいですか？」の提案形式にする
  - 「はい」→そのまま進む / 「いいえ」→自由に回答してよい
- Q&Aの回答を踏まえて `.claude/handoff/spec.md` に書き出す
- **px・色・フォント・テキストの詳細は書かない**（開発担当がFigmaから直接読む）
- 完了したらPMに報告する
- コードは書かない

### 開発担当
**Figmaを直接参照して実装する**
- `.claude/handoff/spec.md` を読んで構造・方針を把握する
- **実装時はFigmaを直接参照する**（px・色・フォント・テキスト・記号は全てFigmaから読む）
- **画像はFigma MCPが提供するURLからのみ取得する（他のソースからの使用禁止）**
- **セクション単位で実装し、完了ごとにFigmaスクリーンショットと比較して自己チェックする**
- 差異があれば即修正してから次のセクションへ進む
- レビュー担当の差異リストを元に修正する
- 完了したらPMに報告する

#### 画像フォーマットルール
Figmaから画像を取得する際は、**Figma内部のフォーマットに応じて以下を厳守する**：

| Figma内部フォーマット | 主な用途 | 保存方法 |
|---|---|---|
| SVG（ベクター） | アイコン・線・図形・ロゴ | `.svg` のまま保存・使用 |
| PNG / JPEG（ラスター） | 写真・背景画像・イラスト | `cwebp` コマンドで `.webp` に変換してから保存 |

**変換コマンド例：**
```bash
cwebp 元ファイル.png -o 保存先.webp
```

**禁止事項：**
- PNGやJPEGの中身を `.webp` という拡張子でそのまま保存しない（ブラウザで表示されない）
- SVGを無理にラスタライズして `.webp` に変換しない（画質劣化・ファイルサイズ増加）

### レビュー担当
- Figmaを読んで現在の実装と比較する
- 差異を全てリストアップする（小さなズレも見逃さない）
- コードは一切触らない・修正しない
- 差異リストを `.claude/handoff/review.md` に上書きしてPMに報告する
- 差異ごとに以下の状態を記載する：
  - `[ ]`：未修正
  - `[x]`：修正確認済み
  - `[-]`：実装上の制約により代替（理由を記載）

---

## 1サイクルの流れ

1. **あなた** → PMにFigmaURLとディレクトリ名を渡す
2. **PM** → 設計担当に指示を出す
3. **設計担当** → FigmaをReadしてQ&Aをまとめ、PMを通じて**あなた**に確認する
4. **あなた** → Q&Aに回答する
5. **設計担当** → 回答を踏まえて構造仕様書を `.claude/handoff/spec.md` に作成してPMに報告
6. **PM** → 開発担当に実装開始を指示する
7. **開発担当** → セクション単位でFigmaを直接参照しながら実装・自己チェックしPMに報告
8. **PM** → **あなた**に初稿完了を報告し確認URLを提示する
9. **あなた** → 初稿を確認してPMにフィードバックする
10. **PM** → レビュー担当に1回目レビューを指示する
11. **レビュー担当** → Figmaと実装を比較して差異リストを `.claude/handoff/review.md` に作成しPMに報告
12. **PM** → 差異リストを確認して開発担当に修正を指示する
13. **開発担当** → 修正してPMに報告
14. **PM** → レビュー担当に2回目レビューを指示する
15. **レビュー担当** → `.claude/handoff/review.md` を上書きしPMに報告
16. **PM** → 差異リストに `[ ]` が残る場合は追加サイクルを実施する。全て `[x]` または `[-]` になったら**あなた**に完了報告・確認URLを提示する
17. **あなた** → 完成物を確認して承認する
18. **PM** → CLAUDE.md改善案・Q&A実績を `.claude/handoff/improvement.md` に作成して**あなた**に提示する
19. **あなた** → 改善案を承認する
20. **PM** → CLAUDE.mdを更新して次のサイクルへ

---

## 完了条件
- ビルドエラーなし
- PC・SP全セクションをFigmaと目視比較済み
- 全アニメーション・ホバー・インタラクション動作確認済み
- `review.md` の全差異が `[x]` または `[-]` であること

---

## 引き継ぎファイル（毎回上書き）
- `.claude/handoff/spec.md` … 設計担当が作成する構造仕様書
- `.claude/handoff/review.md` … レビュー担当が作成する差異リスト
- `.claude/handoff/improvement.md` … PMが作成するCLAUDE.md改善案

---

## improvement.mdに記録するQ&A実績
サイクル終了時に以下の形式で記録する：
```
## Q&A実績
- 質問件数: X件 / 上限10件
- 内訳: アニメーションX件・ホバーX件・SP挙動X件・代替案X件・その他X件
```

---

## スクリーンショット運用ルール

修正作業の進捗管理にスクリーンショットを使用する。

### フォルダ構成
```
.claude/handoff/screenshots/
  cta/        pc.png / sp.png
  header/     pc.png / sp.png
  fv/         pc.png / sp.png
  benefit/    pc.png / sp.png
  authority/  pc.png / sp.png
  voice/      pc.png / sp.png
  solution/   pc.png / sp.png
  lifestyle/  pc.png / sp.png
  features/   pc.png / sp.png
  how-to-use/ pc.png / sp.png
  faq/        pc.png / sp.png
  footer/     pc.png / sp.png
```

### ルール
- 修正着手前に `{セクション名}/` へ `pc.png` / `sp.png` を保存する
- 修正完了後はフォルダごと削除する
- 残りフォルダ＝未完了セクション、空になったら全完了
- 修正指示は「`cta/` を見て修正して」「上から順に全部やって」のように指定できる

---

## 共有スタイル（mock_site共通）

新規mock_siteプロジェクトを作成する際は、必ず以下を `main.scss` の先頭で読み込む：

```scss
@use '../shared/reset';     // box-sizing / scroll / img / a / ul,ol / button
@use '../shared/utilities'; // pc-only / sp-only
```

共有ファイルの置き場所: `src/styles/mock_site/shared/`

---

## ディレクトリ規則
- 画像: `public/mock_site/{ディレクトリ名}/images/`
- コンポーネント: `src/components/mock_site/{ディレクトリ名}/`
- スタイル: `src/styles/mock_site/{ディレクトリ名}/`
- ページ: `src/pages/mock_site/{ディレクトリ名}/`
- レイアウト: `src/layouts/mock_site/{ディレクトリ名}/`
