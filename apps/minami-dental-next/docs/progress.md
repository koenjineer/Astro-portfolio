# 進捗

完了済みの作業の経緯と判断は `progress-log.md`（古い順）。ここには最新の記録と次のアクションだけを置く。

## 2026-09-12 スタッフブログ（ブランチ `feat/minami-blog`）

やったこと：

- スタッフブログ3画面を実装：一覧（`/blog/`・`/blog/page/2/`）・カテゴリー別（`/blog/category/column/` など）・詳細（`/blog/blog-01/` など）。
  ルートはお知らせと同じ5ファイル（`app/blog/`）。見た目は `components/archive/` の部品のままで、新しいスタイルは足していない
- 一覧の組み立て（カテゴリーの確認→ページ分け→無ければ404→画面）を `NewsListPage` から `components/archive/ArchiveListPage.tsx` に移し、
  お知らせ・ブログは記事とカテゴリーを取って渡すだけにした。`<title>` の並べ方も `archiveMetaTitle.ts` の1か所にした。
  整理の前後で書き出したお知らせのHTML（22ページ）を比べ、見える中身が同じことを確認
- ブログの写真11枚を600×315に縮めて同じ名前で差し替え（合計 約1.6MB → 284KB）
- レビュー指摘で2つを1か所にまとめた（動作・書き出しは変わらない）：カテゴリーの絞り込みを `lib/pagination.ts` の `filterByCategory` に
  （「書き出すページ数」と「そのページに並ぶ記事」の条件がずれないように）、サイト名「みなみ歯科」を `lib/clinic.ts` の `SITE_NAME` に
- 進捗の記録が200行を超えるので、完了分を `progress-log.md` に移した（ユーザー決定）
- `lint`・`build` が通り、書き出しに `minami-dental-cms.local` が残らないこと、実機のChrome（PC・SP幅375px）でFigmaと同じ組みであることを確認

判断したこと：

- 1ページ9件（`BLOG_PER_PAGE`。Figmaの一覧が9件＋2ページ目）
- サイドバーはFigmaでもお知らせと同じ組み（クリニックの紹介・新着記事4件・カテゴリー）なので、`ArchiveSidebar` をそのまま使う
- カテゴリーの並びはFigmaどおり「歯科コラム→患者様の声→小児歯科」をコードに持つ（ユーザー決定。詳細は `docs/wordpress.md`）
- 写真は一覧のカードでしか使わないので、PCの表示264×153の2倍が収まる600×315にした（ユーザー決定）
- Figmaの記事一覧の原稿（25494-7440）とWordPressは、タイトル・カテゴリー・並び・写真がすべて一致。
  日付だけ blog-10・11 がFigmaでは「2024.06.19」で、写し間違いとみてWordPressの日付のまま表示している
- 開発サーバーの「1 Issue」（hydrationの不一致）は、Chrome拡張（ColorZilla）が `<body>` に足す属性が原因。サイトのコードとは無関係

## 次のアクション

1. **次は「当院について」を推奨**（固定テキストのページ。Figma PC 25324-8031 / SP 25326-8423）
2. **WordPressの原稿の直し（ユーザー作業・任意）**：ブログの詳細はFigmaだと最後の見出しが「定期検診を受診しましょう」で
   「WEB予約」がリンクだが、WordPressは「まとめ」でリンク無し。blog-10・11 の日付もFigmaと違う（上記）。
   **お知らせの本文の誤字**：見出し（h3）→段落と「ホリ会」「おかけしますは」は2026-09-12に直し済み。
   「させていただきいます」が15件すべてに残っている。管理画面で直す（コードでは書き換えない）
3. 実機のChromeで開き、タブにfaviconが出ていることを確かめる（素材の書き出しの確認の残り）
4. 当院についての後も1ページずつ実装する（作業範囲はページごとにユーザーが決める）。おすすめ順は、
   データ連携のページ（診療案内・スタッフ紹介）→ 最後にホーム
5. 各ページを実装したら、フッターのサイトマップの小項目のリンク先をページ内の見出しへ付け替える
