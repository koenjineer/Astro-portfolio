# 進捗

完了済みの作業の経緯と判断は `progress-log.md`（古い順）。ここには最新の記録と次のアクションだけを置く。

## 2026-09-12 診療案内（ブランチ `feat/minami-medical`）

やったこと：

- `/medical/` を実装：ページ内の目次（一般診療・特殊診療のボタン）→ 診療タイプごとの水色の帯（上下に波）＋診療カード。
  ページ内の部品は `app/medical/_components/`
- 目次のボタンは各カード（`#medical-01`〜`08`）へのページ内リンク。ホバー・フォーカスで地が `main-light` になり下向きの矢印が出る
- カード右上の札は、診療タイプのslugから決める（`general`→保険対象・青、`special`→実費・赤。WPに無いのでコードに持つ。
  知らないタイプが増えたらビルドが止まる）
- フッターのサイトマップ「診療内容」の8項目を各カードへ付け替えた
- 本文の幅の枠 `ContentWidth` を2ページ目で使うので `components/layout/` へ移した
- WPの01〜03の説明文（別の診療の文が入っていた）を、ユーザーが管理画面でFigmaの文章に直した

判断したこと：

- 波は画面幅に合わせて比率を保って伸縮する（1280pxより広い画面でも波が切れない）。下の波を帯に重ねる量と、
  SPの帯の下の余白は画面幅に対する%で持ち、どの幅でも最後のカードに波が食い込まないようにした（レビュー指摘）
- 波が小数のピクセルの高さになり帯とのつなぎ目に細い線が出たので、1pxずつ重ねて消した（実機Chromeで発見）
- 1列表示（1024px未満）のカードの写真は、元の写真（630px）より引き伸ばさないよう630pxで止めた
- 白文字と `main`・`accent` の地の組み合わせは文字の見やすさの基準（WCAG AA 4.5:1）に届かない（約3.3〜3.4:1）が、
  Figmaの色の決まりどおりにした

## 次のアクション

1. **次は当院について**（ユーザー決定。Figma PC 25324-8031 / SP 25326-8423。計画は承認済み）。
   写真は画面の端に付けたまま広い画面では半分の幅まで広げる、Figmaの誤字「勤めて」「期に」は直して書く（どちらもユーザー決定）。
   その後、最後にホーム
2. **WordPressの原稿の直し（ユーザー作業・任意）**：ブログの詳細はFigmaだと最後の見出しが「定期検診を受診しましょう」で
   「WEB予約」がリンクだが、WordPressは「まとめ」でリンク無し。blog-10・11 の日付もFigmaと違う（`docs/wordpress.md`）。
   **お知らせの本文の誤字**：「させていただきいます」が15件すべてに残っている。管理画面で直す（コードでは書き換えない）
3. 実機のChromeで開き、タブにfaviconが出ていることを確かめる（素材の書き出しの確認の残り）
4. フッターのサイトマップの小項目のリンク先をページ内の見出しへ付け替える（スタッフ紹介・診療案内は済み。残りは当院について）
5. `progress-log.md` が200行に近いので、次に記録を移す時に分け方を決める
