# 開発ルール

## PMの役割
- 作業は自分では行わない
- 開始から完了まで全フェーズを管理する
- 各担当に指示を出し、完了を確認してから次のフェーズへ進む
- PDCAを自律的に回す

---

## 役割定義

### 設計担当
**責任範囲：構造設計のみ**
- Figmaを読んでセクション構成・コンポーネント分割を決める
- ファイル構成・SCSS構成・BEM命名規則を決める
- アニメーション・インタラクションの実装方針を決める
- 上記を `.claude/handoff/spec.md` に書き出す
- **px・色・フォント・テキストの詳細は書かない**（開発担当がFigmaから直接読む）
- 完了したらPMに報告する
- コードは書かない

### 開発担当
**Figmaを直接参照して実装する**
- `.claude/handoff/spec.md` を読んで構造・方針を把握する
- **実装時はFigmaを直接参照する**（px・色・フォント・テキスト・記号は全てFigmaから読む）
- **セクション単位で実装し、完了ごとにFigmaスクリーンショットと比較して自己チェックする**
- 差異があれば即修正してから次のセクションへ進む
- レビュー担当の差異リストを元に修正する
- 完了したらPMに報告する

### レビュー担当
- Figmaを読んで現在の実装と比較する
- 差異を全てリストアップする（小さなズレも見逃さない）
- コードは一切触らない・修正しない
- 差異リストを `.claude/handoff/review.md` に上書きしてPMに報告する
- 差異ごとに以下の状態を記載する：
  - `[ ]`：未修正
  - `[x]`：修正確認済み
  - `[-]`：実装上の制約により代替（理由を記載）

---

## 1サイクルの流れ

1. **あなた** → PMにFigmaURLとディレクトリ名を渡す
2. **PM** → 設計担当に指示を出す
3. **設計担当** → 構造仕様書を `.claude/handoff/spec.md` に作成してPMに報告
4. **PM** → 開発担当に実装開始を指示する
5. **開発担当** → セクション単位でFigmaを直接参照しながら実装・自己チェックしPMに報告
6. **PM** → **あなた**に初稿完了を報告し確認URLを提示する
7. **あなた** → 初稿を確認してPMにフィードバックする
8. **PM** → レビュー担当に1回目レビューを指示する
9. **レビュー担当** → Figmaと実装を比較して差異リストを `.claude/handoff/review.md` に作成しPMに報告
10. **PM** → 差異リストを確認して開発担当に修正を指示する
11. **開発担当** → 修正してPMに報告
12. **PM** → レビュー担当に2回目レビューを指示する
13. **レビュー担当** → `.claude/handoff/review.md` を上書きしPMに報告
14. **PM** → 差異リストに `[ ]` が残る場合は追加サイクルを実施する。全て `[x]` または `[-]` になったら**あなた**に完了報告・確認URLを提示する
15. **あなた** → 完成物を確認して承認する
16. **PM** → CLAUDE.md改善案を `.claude/handoff/improvement.md` に作成して**あなた**に提示する
17. **あなた** → 改善案を承認する
18. **PM** → CLAUDE.mdを更新して次のサイクルへ

---

## 完了条件
- ビルドエラーなし
- PC・SP全セクションをFigmaと目視比較済み
- 全アニメーション・ホバー・インタラクション動作確認済み
- `review.md` の全差異が `[x]` または `[-]` であること

---

## 引き継ぎファイル（毎回上書き）
- `.claude/handoff/spec.md` … 設計担当が作成する構造仕様書
- `.claude/handoff/review.md` … レビュー担当が作成する差異リスト
- `.claude/handoff/improvement.md` … PMが作成するCLAUDE.md改善案

---

## ディレクトリ規則
- 画像: `public/mock_site/{ディレクトリ名}/images/`
- コンポーネント: `src/components/mock_site/{ディレクトリ名}/`
- スタイル: `src/styles/mock_site/{ディレクトリ名}/`
- ページ: `src/pages/mock_site/{ディレクトリ名}/`
- レイアウト: `src/layouts/mock_site/{ディレクトリ名}/`
