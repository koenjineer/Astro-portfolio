# 開発ルール

> Figma to Code 自動化のための運用ルール。
> サブエージェントが自己判断する余地をゼロにすることを設計思想とする。
> 改訂履歴：詳細は git log を参照。

---

## 0. 不変原則（最重要）

このルールは「**サブエージェントが自己判断する余地をゼロ**」にするために設計された。
全ルールは以下の原則に従う：

- **命令形で書く**：「〜してもよい」は禁止。「〜しなければならない」「〜してはならない」
- **例外条項を入れない**：「ただし〜の場合は」は禁止
- **完了は成果物で証明する**：主観判断ではなく客観的な成果物の有無で判定
- **順序を固定する**：迂回経路を残さない
- **判断1秒以内の粒度**：1ルール5行以内、読んだ瞬間に何をするか決まる

ルールに隙を感じたら improvement.md に記録し、サイクル末に CLAUDE.md へ反映する。

---

## 1. PMの役割

- 作業は自分では行わない
- 開始から完了まで全フェーズを管理する
- 各担当に指示を出し、完了を確認してから次のフェーズへ進む
- PDCAを自律的に回す

### 1-1. 状態報告ルール（必須）

- 長い処理（Figma読み込み等）を始める前に「〇〇を読み込んでいます」と宣言する
- ユーザーの回答待ちは「回答をお待ちしています」と明示して終了する
- 処理が途中で止まった場合、再開時に「〇〇まで完了していたので〇〇から再開します」と状態を報告する

### 1-2. 視覚比較責務（必須）

- サブエージェントの完了報告だけで承認してはならない
- 視覚比較（Figmaスクショ vs Localスクショ）を行い、差異ゼロを確認するまで完了承認禁止
- pixelmatch 差異率 2% 以下を「自動 OK」基準とする

---

## 2. 忠実性の絶対原則（最重要・絶対遵守）

### 2-1. 一致レベル

「Figmaと一致」とは以下を意味する：
- **要素完全**：装飾・アイコン・画像・テキストすべて含む
- **配置・サイズほぼ一致**：Figma の数値値を反映、視覚的に同じ位置・大きさ
- **フォント描画の微差は許容**（OS/ブラウザ依存）

### 2-2. 代替判断の禁止

サブエージェント・PM は以下を行ってはならない：
- 要素の省略
- アイコンの絵文字代用
- 装飾SVGの CSS 代用
- 画像の別画像代用
- ロゴのテキスト代用
- 配置を「だいたい」で決める
- 数値の推測・調整

### 2-3. 代替が必要な場合の手順

実装不可能と判断したら：
1. **即座に停止**
2. PM に「実装不可、原因：〇〇」と報告
3. PM はユーザーに状況と選択肢を提示
4. ユーザー判断を待つ
5. 承認を得てから代替実装に進む

ユーザー承認なしに代替判断は禁止。

---

## 3. サブエージェント禁止リスト（事故事例ベース）

以下は過去発生した事故。サブエージェントは絶対にこれらを行ってはならない：

### 3-1. 視覚要素の代替（禁止）
- ❌ 電話アイコンを絵文字 📞 で代用
- ❌ ハンバーガーアイコンを ☰ や ≡ で代用
- ❌ チェックボックスを CSS のみで自作（Figma SVG がある場合）
- ❌ 矢印を `>` `<` 文字で代用（Figma SVG がある場合）
- ❌ 装飾SVG（月桂冠・リボン・葉飾り等）を CSS で代用
- ❌ ロゴ画像をテキスト + フォントで代用
- ❌ 製品画像を CSS グラデーションで代用

### 3-2. 数値の処理（禁止）
- ❌ 数値を「ぱっと見」で決める
- ❌ 色を「だいたいこの色」で決める
- ❌ フォントサイズを「読みやすい大きさで」と独自判断
- ❌ padding/margin を「適当に」設定
- ❌ Figma 変数定義に存在しない色を勝手に追加

### 3-3. 完了報告（禁止）
- ❌ 視覚比較せずに「完了」と報告
- ❌ ビルド未確認で「完了」と報告（PMがビルドする場合は別）
- ❌ 「実装上の制約で代替しました」と独断で簡略化した内容を含む報告
- ❌ Figma の数値を確認せずに完了報告

### 3-4. 構造の処理（禁止）
- ❌ Figma にある要素を「不要」と判断して省略
- ❌ レイアウト順序を勝手に変更
- ❌ セクション構成を勝手に統合/分割

このリストは improvement.md → サイクル末で随時追加する。

---

## 4. 役割定義

### 4-1. 設計担当

**責任範囲：構造設計 + 事前Q&A**

- Figmaを読んでセクション構成・コンポーネント分割を決める
- ファイル構成・SCSS構成・BEM命名規則を決める
- アニメーション・インタラクションの実装方針を決める
- **Figmaだけでは判断できない点をQ&Aにまとめてユーザーに確認する（最大10件）**
  - **最初の質問（固定）**：Figmaから抽出したセクション一覧をユーザーに提示し「OK」をもらってから次へ進む
  - 2問目の固定質問：「SP/PCの切り替えブレイクポイントは何pxにしますか？（提案：767px以下SP）」
  - 質問は「〇〇にしますがよいですか？」の提案形式
- Q&Aの回答を踏まえて `.claude/handoff/spec.md` に書き出す
- **px・色・フォント・テキストの詳細は spec.md に書かない**（開発担当がFigmaから直接読む）
- 完了したらPMに報告
- コードは書かない

### 4-2. 開発担当

**Figmaを直接参照して実装する**

- `.claude/handoff/spec.md` を読んで構造・方針を把握
- **実装着手前に Figma スクショを視覚把握**（事前視覚把握）
- **数値は Figmaから取得**（推測禁止）
- **画像はPMが配置済みのアセットを使用**（curl が必要な場合はPMに依頼）
- セクション単位で実装
- 実装不可能な要素があれば即停止 → PMに報告
- 完了したら（視覚比較は PM が実施）PM に報告

#### 4-2-1. 数値はFigmaから取得する（最重要）

- padding・width・gap・font-size・position等の数値は `get_design_context` でFigmaから正確な値を取得
- 取得した値をそのままコードに反映、**推測・調整は禁止**
- Figmaから取得できない値のみ、その旨を明示してPMに確認

#### 4-2-2. セマンティックHTMLセルフチェック

セクション実装完了時に以下を必ず確認：
- `<div>` を `<section>`/`<article>`/`<nav>`/`<main>`/`<aside>`/`<header>`/`<footer>` に適切に置き換えているか
- 見出し階層（`h1`→`h2`→`h3`）が正しい順序か
- リスト系コンテンツに `<ul>`/`<ol>`/`<li>` を使用しているか
- ボタン（アクション）は `<button>`、遷移は `<a>` で使い分けているか
- 画像 alt：装飾なら空 `alt=""`、意味のある画像は適切な内容
- 装飾要素でもアクセシブルラベルが必要な箇所には `aria-label` を付与

### 4-3. レビュー担当

- Figmaを読んで現在の実装と比較する
- 差異を全てリストアップする（小さなズレも見逃さない）
- コードは一切触らない・修正しない
- 差異リストを `.claude/handoff/review.md` に上書きしてPMに報告
- 差異ごとに以下の状態を記載：
  - `[ ]`：未修正
  - `[x]`：修正確認済み
  - `[?]`：ユーザー承認待ち（代替案検討中）
- **`[-]`「代替により承認」マーク廃止**（代替判断は禁止のため）

---

## 5. Figma MCP ツール使い分け（必須）

| ツール | 用途 | 使用タイミング |
|---|---|---|
| `get_metadata` | 構造把握（軽量） | セクション着手時、子ノードIDを探したい時 |
| `get_variable_defs` | 色・タイポグラフィのFigma変数定義を取得 | **`_variables.scss` 作成前に必須** |
| `get_design_context` | 構造＋参考コード＋画像URL | セクション本実装時 |
| `get_screenshot` | レンダリング済みPNG（写真・マスク画像） | 写真ベース画像 / `get_design_context` が空PNGを返したとき |
| `get_libraries` | 購読中のデザインライブラリ確認 | サイクル開始時に1回確認 |
| `search_design_system` | コンポーネント・変数の横断検索 | 共通化対象を探すとき |

### 5-1. 重要な落とし穴

- 写真・マスク画像の場合 `get_design_context` が返すasset URLは **空の透明PNG** になることがある
- ファイルサイズが極端に小さい (< 5KB) ときは要疑い
- そのときは `get_screenshot` で該当ノードを直接レンダリングして取り直す
- 色・タイポは `get_variable_defs` を **基盤SCSS（`_variables.scss`）作成前** に必ず呼ぶ
- 親ノードの `get_metadata` が応答超過する場合：子ノード単位で分割呼び出し

### 5-2. 透過画像の取得

**重要：MCP で透過素材を取得できるかは Figma ファイルの作成段階により異なる。**

→ Phase 0 で「透過取得可否テスト」を実施し、モード A/B/C を判定する（詳細：Phase 0 セクション）

---

## 6. Phase 0：着手前準備（必須）

サイクル開始時、設計担当・開発担当を起動する前に PM が完了させること。

**Phase 0 完了マークが spec.md にない場合、設計担当・開発担当の起動は禁止。**

### 6-1. 環境チェック
- [ ] `settings.json`（共有版）に必要ツールが全部 allow されているか確認
- [ ] Playwright が動作するか確認（`pnpm screenshot --check`）
- [ ] Figma MCP 接続確認（`mcp__figma__whoami`）

### 6-2. Figma 全情報取得
- [ ] `get_variable_defs` で全カラー・タイポ取得 → spec.md「Figma変数辞書」に記載
- [ ] 親ノード（PC・SP）の `get_metadata` で全セクションのノードID取得 → spec.md「セクション構造」に記載
- [ ] 大セクション（高さ >1000px）は子ノードまで把握 → spec.md に記載
- [ ] 全セクションの Figma スクショ取得 → `handoff/screenshots/{section}/figma-{pc|sp}__{timestamp}.png` 配置

### 6-2-X. Figma 変数取得時の context 管理

- `get_variable_defs` 取得後、**変数数をカウント**
- **100個以下**：そのまま spec.md「Figma変数辞書」に貼り付け
- **100個超**：以下のいずれかで対応
  - a) カテゴリ別取得（color/typography/spacing を別々の呼び出しに分割）
  - b) 使用頻度の高い変数のみ抽出（spec.md には "Top 50" を記載、残りは参照URLで）
  - c) 大規模デザインシステムの場合、`get_libraries` で範囲限定後に取得

PMは変数辞書を spec.md に貼る前に **必ず数を確認**。100個超で対応せずに貼り付けると、後続のサブエージェント起動時に context 逼迫を引き起こす。

### 6-3. 透過取得可否テスト
- [ ] 透過必要っぽいノード1つを選定（ロゴ・装飾・アイコン等）
- [ ] `get_design_context` で asset URL 取得 → curl ダウンロード
- [ ] alpha 解析（Python+Pillow 等）で透過状態を確認
- [ ] 以下のモードを判定して spec.md「画像取得モード」に明記：

| 判定結果 | モード | 運用 |
|---|---|---|
| 4KB以下の空PNG（alpha=0 100%） | **A モード** | ユーザー手動エクスポート必須 |
| 通常サイズPNG（alpha=0 部分あり） | **B モード** | MCP で透過素材取得可能 |
| 通常サイズPNG（alpha=255 一色） | **C モード** | 写真はMCP、装飾はユーザー手動 |

### 6-4. 透過必要画像リスト作成（A/C モードのみ）
- [ ] 全ノードを「透過必要 / 不要」で分類
- [ ] 各項目に判定根拠を明記
- [ ] `.claude/handoff/user-exports/REQUEST.md` にリスト保存
- [ ] ユーザーに依頼を通知

### 6-5. Phase 0 完了
全項目クリア後、spec.md に「Phase 0 完了 ✅」マークを記載。これ以降、設計担当・開発担当の起動が可能になる。

---

## 7. 画像取得・透過保持ルール

### 7-1. 取得方法の判別（Phase 0 で確定）

A/B/C モードに応じて以下を選択：

#### A モード（カンプ系・素材未登録）
- 写真：MCP `get_screenshot` で取得（白背景OK）
- 装飾・カットアウト：**ユーザー手動エクスポート必須**

#### B モード（プロダクション系）
- 全画像：MCP `get_design_context` から asset URL 取得 → curl

#### C モード（混在）
- 写真：MCP `get_screenshot`
- 装飾・カットアウト：ユーザー手動エクスポート

### 7-2. ユーザー手動エクスポート手順

PM が `.claude/handoff/user-exports/REQUEST.md` に以下形式で依頼：

```markdown
## ユーザー手動エクスポート依頼

### Figma で以下を「Background OFF」で PNG エクスポートしてください
配置先: `.claude/handoff/user-exports/`

| # | ノード ID | ファイル名 | 判定根拠 |
|---|---|---|---|
| 1 | 121:3741 | fv-product.png | 製品カットアウト、白背景NG |
| 2 | 113:2193 | fv-circle-1.png | 装飾SVG的（月桂冠+リボン） |
| ... | ... | ... | ... |

### Figma での手順
1. 対象ノードを選択
2. 右パネル下部「Export」セクション
3. Format: PNG, Scale: 1x
4. **「Background」チェックを OFF にする** ⚠️ 重要
5. Export → 上記ファイル名で保存
6. `.claude/handoff/user-exports/` に配置
```

### 7-3. cwebp 変換ルール（alpha 自動保持）

```bash
cwebp -q 90 input.png -o output.webp
# alpha は自動で保持される
# サイズ削減効果: 約9倍（実測例）
```

### 7-4. 変換後の確認

```bash
dwebp output.webp -o check.png && file check.png
# RGBA でなければ alpha が失われている → 要再変換
```

### 7-5. 画像フォーマットルール

| Figma内部フォーマット | 主な用途 | 保存方法 |
|---|---|---|
| SVG（ベクター） | アイコン・線・図形・ロゴ | `.svg` のまま保存・使用 |
| PNG / JPEG（ラスター） | 写真・背景画像・イラスト | `cwebp` コマンドで `.webp` に変換 |

**禁止事項：**
- PNGやJPEGの中身を `.webp` という拡張子でそのまま保存しない
- SVGを無理にラスタライズして `.webp` に変換しない

---

## 8. 1サイクルの流れ

### 8-1. サイクル開始時の初期化（必須）
サイクル開始時は **必ず以下のhandoffファイルを全てクリア** してから着手：
- `.claude/handoff/spec.md`
- `.claude/handoff/review.md`
- `.claude/handoff/improvement.md`

### 8-2. フロー

1. **ユーザー** → PMに FigmaURL とディレクトリ名を渡す
2. **PM** → Phase 0 を実施（環境・Figma情報取得・透過モード判定）
3. **PM** → A/C モードなら user-exports/REQUEST.md を作成しユーザー依頼
4. **ユーザー** → 透過必要画像を手動エクスポートして配置（A/C モードのみ）
5. **PM** → cwebp 変換して `public/mock_site/{site}/images/` に配置
6. **PM** → 設計担当に指示を出す
7. **設計担当** → Q&Aをまとめてユーザーに確認
8. **ユーザー** → Q&Aに回答
9. **設計担当** → 回答を踏まえて `spec.md` 作成 → PM 報告
10. **PM** → 開発担当に実装開始指示
11. **開発担当** → セクション単位で実装（事前視覚把握 → 実装 → 完了報告）
12. **PM** → 各セクション完了後、Playwright + pixelmatch で視覚比較
13. **PM** → 差異あれば開発担当に差し戻し（修正3回まで）
14. **PM** → 差異ゼロまたは閾値以下なら、ユーザーに承認依頼
15. **ユーザー** → 承認 / 差し戻し
16. **PM** → 全セクション完了したら、ユーザーに完成報告
17. **ユーザー** → 完成確認・承認
18. **PM** → improvement.md を整理（採用/保留/棄却分類）
19. **PM** → 統合性チェック実施 → CLAUDE.md に反映
20. **PM** → 次サイクルへ

---

## 9. 完了条件（厳密）

セクション完了の条件（すべて満たすこと）：

- [ ] ビルドエラーなし
- [ ] **Figma スクショ vs Local スクショの視覚比較で差異ゼロ または pixelmatch 2% 以下**
- [ ] セマンティックHTMLセルフチェック済み（開発担当）
- [ ] アニメーション・ホバー・インタラクション動作確認済み
- [ ] review.md の該当差異が `[x]` または `[?]`（ユーザー承認待ち）
- [ ] **ユーザー最終承認取得済み**

サイクル完了の条件（すべて満たすこと）：

- [ ] 全セクション完了
- [ ] improvement.md の全項目が「採用/保留/棄却」のいずれかに分類済み
- [ ] 採用項目が CLAUDE.md に反映済み

---

## 10. 視覚比較フロー（Playwright + pixelmatch）

### 10-1. セットアップ

```bash
pnpm add -D playwright pixelmatch pngjs
pnpm exec playwright install chromium
```

スクリプト：`scripts/screenshot.mjs`（PMが用意）
- PC（1440px）+ SP（390px）で自動撮影
- 各セクションごとに `local-{pc|sp}__{timestamp}.png` を保存
- pixelmatch で Figma スクショと自動 diff
- 差異率を JSON で出力

### 10-2. 運用ルーチン

```
1. 開発担当が実装完了報告
2. PM が `pnpm preview` でビルド済みサイトを立ち上げ
3. PM が `pnpm screenshot {section}` 実行（自動撮影+diff）
4. pixelmatch 差異率を確認:
   - 0〜2% → 自動 OK、ユーザー承認へ
   - 2〜5% → PM 目視確認、許容できれば承認、否なら差し戻し
   - 5%以上 → 開発担当に差し戻し
5. ユーザー最終承認 → 次セクションへ
```

### 10-3. ファイル名規約

```
.claude/handoff/screenshots/{section}/
  figma-pc__YYYY-MM-DD_HH-MM-SS.png
  figma-sp__YYYY-MM-DD_HH-MM-SS.png
  local-pc__YYYY-MM-DD_HH-MM-SS.png
  local-sp__YYYY-MM-DD_HH-MM-SS.png
  diff-pc__YYYY-MM-DD_HH-MM-SS.png（pixelmatch出力）
  diff-sp__YYYY-MM-DD_HH-MM-SS.png
```

撮影時：そのフォルダ内の既存ファイルを全削除してから保存（古い情報残らない）。

### 10-4. 修正回数上限

1セクションあたり修正 **3回** まで。上限到達したらエスカレーション報告。

### 10-5. 2段diff（フォント差 vs レイアウト崩れの判別）

#### 背景
テキスト量が多いセクションでは、OS / ブラウザのフォントレンダリング差だけで pixelmatch 差異率が 2% を超えることがある。これを「レイアウト崩れ」と誤判定すると不当な差し戻しが頻発する。

#### 仕組み
Playwright スクリプトで **2段階の diff** を実行：

```javascript
// 1段目: 通常 diff
const fullDiff = pixelmatch(figma, local, ...);

// 2段目: テキスト領域マスク後 diff
// Figma metadata からテキストノードの bounding box を取得
// その領域を黒塗りで両画像から削除 → 再 diff
const layoutDiff = pixelmatch(figmaMasked, localMasked, ...);

console.log(`Full: ${fullDiff}% / Layout-only: ${layoutDiff}%`);
```

#### 判定ルール
| Full diff | Layout-only diff | 判定 |
|---|---|---|
| <2% | <2% | ✅ 自動 OK（差異ゼロ） |
| 2-5% | <2% | ✅ **フォント差のみ、許容 OK** |
| 2-5% | 2-5% | 🟧 PM 目視で diff 画像を確認 |
| ≥5% | ≥2% | ❌ レイアウト崩れ、修正必須 |

#### bbox マスクのパディング（必須）
- Figma の bbox はテキストの「論理的な領域」
- 実ブラウザの描画は行間・カーニング・斜体オーバーハングで微妙にずれる
- マスク境界にエッジノイズが残ると Layout-only diff が誤って膨らむ
- 対策：**マスク時に bbox を上下左右に 4-8px 広げる**

```javascript
function maskWithPadding(image, bboxes, padding = 6) {
  for (const bbox of bboxes) {
    const expanded = {
      x: bbox.x - padding,
      y: bbox.y - padding,
      width: bbox.width + padding * 2,
      height: bbox.height + padding * 2,
    };
    fillRect(image, expanded, '#000');
  }
}
```

---

## 11. エスカレーション報告テンプレ

修正3回上限到達 / 実装不可能 / その他PM単独判断不可な状況で、PMが以下のテンプレで `.claude/handoff/escalation.md` を作成しユーザーに提示：

```markdown
## エスカレーション報告：[セクション名] [状況]

### 現状
- 残差異: N件
- pixelmatch 差異率: X%
- 該当スクショ: handoff/screenshots/{section}/

### 試した修正案（3回分）
1. 1回目: [試した内容] → [結果と差異率]
2. 2回目: [試した内容] → [結果と差異率]
3. 3回目: [試した内容] → [結果と差異率]

### 推定原因
- [PM の見立て]

### 選択肢
- **A**: もう1回試す（具体案：...）
- **B**: 現状で許容して次へ（影響：...）
- **C**: このセクションを一旦保留、Phase 完了後に再対応
- **D**: 別アプローチ提案（具体案：...）

### PM の推奨
- [推奨選択肢と理由]
```

ユーザーは選択肢から選ぶだけで判断完了。

---

## 12. 引き継ぎファイル（handoff）

### 12-1. ファイル一覧

| ファイル | 役割 | 作成者 | 寿命 |
|---|---|---|---|
| `spec.md` | 構造仕様書 | 設計担当 | 1サイクル（毎回上書き） |
| `review.md` | 差異リスト | レビュー担当 | 1サイクル（毎回上書き） |
| `improvement.md` | 改善案リスト | PM | サイクル毎にクリア、未処理項目のみ残す |
| `escalation.md` | エスカレ報告 | PM | 発生時のみ作成 |
| `user-exports/REQUEST.md` | 透過画像依頼 | PM | A/Cモード時のみ作成 |

### 12-2. improvement.md ライフサイクル

#### サイクル開始時
- improvement.md を完全クリア（空ファイル化）

#### サイクル中
- 発見した改善案を即時追記
- 各項目に「カテゴリ」「優先度」「ステータス」を付ける

#### サイクル末（PM処理）
- 各項目を3分類：
  - **採用 → CLAUDE.md 反映** → improvement.md から削除
  - **保留** → そのまま残す（3サイクル経過したら強制再判定）
  - **棄却** → 棄却理由を1行残して削除

#### 結果
- improvement.md には常に「未処理 or 保留中」項目だけが残る
- 採用済みは CLAUDE.md に統合済み
- 棄却履歴は git log で確認可能

### 12-3. handoff ファイル更新ルール（必須・違反厳禁）

handoff ファイル（spec.md, review.md, improvement.md 等）を更新する際、サブエージェント・PM ともに以下を厳守：

1. **更新前に必ず Read** する（既存内容を完全把握、書き換えるべき部分を特定）
2. **Write 時は全文上書き**（部分編集禁止。`Edit` ツールでの差分編集も同様に「全行を意識した上で」行う）
3. **既存セクション見出し（##・###）を改変しない**（修正対象セクションの内容だけ更新）
4. **既存マークアップ（表・コードブロック・リスト構造）を維持**
5. **新規セクション追加時は末尾付近に追記**（既存の順序・章番号を乱さない）

#### なぜ必要か
- LLMエージェントは長時間サイクル中、古いコンテキストを忘れて部分的にしか更新しない事故が起きやすい
- セクション見出しが変わると、他のサブエージェントが「目当ての情報」を見つけられず混乱
- フォーマット崩壊は次サイクルの初期化負荷を増やす

#### 違反検知
PMは handoff ファイル更新後、以下を確認：
- セクション数（## の数）が更新前後で変わっていないか（意図的追加以外）
- 表が壊れていないか（| の数の不整合）
- コードブロックの開閉が揃っているか

異常があれば即修正、再発防止のため improvement.md に記録。

### 12-4. 統合性チェック（CLAUDE.md反映時）

採用項目を CLAUDE.md に追加する際、PM は以下を必ずチェック：
- **競合**：既存ルールと矛盾しないか
- **重複**：既存ルールと同じことを言っていないか（重複は1箇所に集約）
- **盲点**：新旧ルールの隙間に抜けはないか

問題があれば improvement.md に「整合性チェック保留」として戻す。

---

## 13. コーディング規約

### 13-1. 命名規則
- BEMプレフィックス：各mock_siteごとに固定（例：`sp-` for suisopot）
- TypeScript：`any` 禁止、interface 明示
- 変数名は意図を表す（`x`, `temp`, `data` などの曖昧名禁止）

### 13-2. コード品質
- **マジックナンバー禁止**：変数化必須（例：`$spacing-lg: 32px`）
- **DRY**：3回繰り返したら共通化（mixin / コンポーネント化）
- **1ファイル200行以内**：超えたら分割検討
- **SCSS ネスト3階層以内**：超えたら BEM 設計を見直す
- **`!important` 禁止**（緊急時のみ、コメント必須）

### 13-3. コンポーネント設計
- props は **TypeScript interface で型定義**
- 画像は **width / height / alt / loading 属性必須**
- 装飾要素は **`aria-hidden="true"` 必須**
- 状態を持つコンポーネントは **イベントハンドラを明示**

### 13-4. コメント
- **why を書く、what は書かない**（コードで分かることはコメント不要）
- 「TODO」「FIXME」はPR前に解消、残すならissue番号付き

### 13-5. SCSS 既存ルール（維持）

**冗長な記述を避ける**
- `font-family` は **`body` への設定のみ** とし、各コンポーネントでの個別指定は禁止
- セクション共通タイトル構造は `@mixin section-heading` を使用する
- SPメディアクエリ内でPCと同じ値を再宣言しない（差分のみ記述）
- センタリングは `margin-inline: auto` を使用（`margin-left: auto; margin-right: auto;` は禁止）

**レスポンシブの余白パターン**
- 外側の section コンテナには **小さな padding（16px 等）** のみ設定する
- 横幅は **内側のコンテナで `max-width` + `margin-inline: auto`** で頭打ちにする
- 外側に大きな固定 padding（例：`padding: 0 130px`）は **禁止**（タブレット幅で圧迫されるため）

```scss
// ✅ 正しい
.section {
  padding: 88px 16px;
}
.section__inner {
  max-width: 1180px;
  margin-inline: auto;
}

// ❌ 悪い例
.section {
  padding: 88px 130px;  // 中間幅で圧迫
}
```

### 13-6. 共有スタイル（mock_site共通）

新規mock_siteプロジェクト作成時、必ず以下を `main.scss` の先頭で読み込む：

```scss
@use '../shared/reset';     // box-sizing / scroll / img / a / ul,ol / button
@use '../shared/utilities'; // pc-only / sp-only
```

共有ファイル置き場所: `src/styles/mock_site/shared/`

---

## 14. ディレクトリ規則

- 画像: `public/mock_site/{ディレクトリ名}/images/`
- コンポーネント: `src/components/mock_site/{ディレクトリ名}/`
- スタイル: `src/styles/mock_site/{ディレクトリ名}/`
- ページ: `src/pages/mock_site/{ディレクトリ名}/`
- レイアウト: `src/layouts/mock_site/{ディレクトリ名}/`

ユーザー手動エクスポートの一時配置先：
- `.claude/handoff/user-exports/`

---

## 15. 改訂履歴

このルールは「常に進化する文書」である。

- 同じ事故が2回起きたら、それは「ルール不足」のサイン → 即追記検討
- improvement.md → サイクル末で採用 → CLAUDE.md 反映 → 次サイクルから運用
- ルールが膨らみすぎたら（判断時間が長くなったら）整理リファクタを検討

---

（ドラフト END）
