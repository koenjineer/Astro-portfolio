# 進捗

完了済みの作業の経緯と判断は `progress-log.md`（古い順）。2026-09-10〜11の分は `progress-log-archive.md`。
ここには最新の記録と次のアクションだけを置く。

## 2026-09-12 当院について（ブランチ `feat/minami-about`）

やったこと：

- `/about/` を実装：ポリシーと特徴（写真＋文章のブロック2つ）→ 院内の様子（正方形の写真6枚）。
  ページ内の部品は `app/about/_components/`
- 文章はWPに無いのでコードに持つ。Figmaの誤字2か所を直した（「勤めて」→「努めて」、「期に」→「機に」。ユーザー決定）
- 写真8枚を `public/images/about/` へ（`policy-01・02.webp` 640×438、`clinic-01〜06.webp` 640×640。`cwebp -q 90`）
- フッターのサイトマップ「ポリシーと特徴」→ `/about/#policy`、「当院の様子」→ `/about/#clinic`。
  これで全ページの小項目の付け替えが終わった
- 完了分の記録が200行に近づいたので、`progress-log.md`（直近）と `progress-log-archive.md`（2026-09-10〜11）に分けた（ユーザー決定）

判断したこと：

- 写真は画面の端に付けたまま、幅は**640pxで止める**（Figmaの元の写真が640×438しかなく、それ以上は引き伸ばしでぼやけるため。ユーザー決定）
- 1281px以上では文章を画面の中央寄りへ動かし、左右の空白の偏りを目立たなくした（寄せ量は「幅−1280px」の半分。
  1280px以下はFigmaのまま。ユーザーは「まかせる」）
- Figmaに無いタブレット幅（768〜1023px）は、SPの組みのまま本文と写真を640pxで止めて写真側へ寄せた
  （上限が無いと本文1行が983pxまで伸びて読みにくかった。PCの組み1140pxはこの幅に入らないので横並びにはしない）
- 水色の飾りは右端を画面の端に合わせて必ずはみ出させる（左からの距離＋固定幅だと、広い画面と567〜1023pxで
  本来は画面の外にあるはずの輪郭が見えてしまった）
- 院内の写真は中央で正方形に切って640×640（PC表示317pxの2倍が収まる大きさ）
- ポリシーの写真には短い説明（代替テキスト）を付けた。診療案内のWPの写真は `alt=""` だが、こちらは固定の写真で中身を説明できるため

## 次のアクション

1. **次はホーム**（最後のページ。Figma PC 25322-22876 / SP 25324-7238）。
   `app/page.tsx` は今もWordPressとの疎通確認用の仮ページなので、丸ごと置き換える
2. **WordPressの原稿の直し（ユーザー作業・任意）**：ブログの詳細はFigmaだと最後の見出しが「定期検診を受診しましょう」で
   「WEB予約」がリンクだが、WordPressは「まとめ」でリンク無し。blog-10・11 の日付もFigmaと違う（`docs/wordpress.md`）。
   **お知らせの本文の誤字**：「させていただきいます」が15件すべてに残っている。管理画面で直す（コードでは書き換えない）
3. 実機のChromeで開き、タブにfaviconが出ていることを確かめる（素材の書き出しの確認の残り）
