# 開発ルール

> Figma to Code 自動化のための運用ルール。
> サブエージェントが自己判断する余地をゼロにすることを設計思想とする。

---

## 0. 不変原則（最重要）

このルールは「**サブエージェントが自己判断する余地をゼロ**」にするために設計された。

### 0-1. 記述原則（各ルールの書き方）

- **命令形で書く**：「〜してもよい」は禁止。「〜しなければならない」「〜してはならない」
- **例外条項を入れない**：「ただし〜の場合は」は禁止
- **完了は成果物で証明する**：主観判断ではなく客観的な成果物の有無で判定
- **順序を固定する**：迂回経路を残さない
- **判断1秒以内の粒度**：1ルール5行以内、読んだ瞬間に何をするか決まる
- **200行ルール**：CLAUDE.mdを含むすべてのドキュメントは200行を超えたら分割する。超えたまま放置禁止。

### 0-2. コンテキスト維持の4原則

**上位目的：全情報は精度向上に貢献しなければならない。**
精度を下げる失敗は「**欠落**（必要な情報が無い／読まれない）」と「**過剰**（情報が多すぎて読み切れない）」の2つ。下表はその防御であり、**書き込み時（サイクル末の improvement.md 反映直後）に必ず適用する**。

| 原則 | 判定（1秒） | 動作 |
|---|---|---|
| **階層性** | 常に読まれる場所にあるか | CLAUDE.md はナビ＋絶対原則のみ。詳細は `.claude/rules/` へ置く |
| **整合性** | 既存と矛盾・陳腐化していないか | 追記時に古い記述を削除し、競合を解消する |
| **有効性** | これが無いと精度が下がるか | NO なら削除する |
| **原則化** | 上位原則から導出できる具体例か | YES なら書かない。同じ事故が再発したら原則を1つ追加する |

ルールに隙を感じたら improvement.md に記録し、サイクル末に CLAUDE.md へ反映する。

---

## 1. PMの役割

- 作業は自分では行わない
- 開始から完了まで全フェーズを管理する
- 各担当に指示を出し、完了を確認してから次のフェーズへ進む
- PDCAを自律的に回す
- サブエージェントの完了報告だけで承認してはならない。視覚比較（Figmaスクショ vs Localスクショ）で差異ゼロを確認するまで完了承認禁止

詳細な役割定義・サブエージェント仕様 → `.claude/rules/roles.md`

---

## 2. 忠実性の絶対原則（最重要・絶対遵守）

### 2-1. 一致レベル

「Figmaと一致」とは以下を意味する：
- **要素完全**：装飾・アイコン・画像・テキストすべて含む
- **配置・サイズほぼ一致**：Figma の数値を反映、視覚的に同じ位置・大きさ
- **フォント描画の微差は許容**（OS/ブラウザ依存）

### 2-2. 代替判断の禁止

サブエージェント・PM は以下を行ってはならない：
- 要素の省略 / アイコンの絵文字代用 / 装飾SVGのCSS代用
- 画像の別画像代用 / ロゴのテキスト代用
- 配置を「だいたい」で決める / 数値の推測・調整

### 2-3. 代替が必要な場合の手順

1. **即座に停止**
2. PM に「実装不可、原因：〇〇」と報告
3. PM はユーザーに状況と選択肢を提示 → ユーザー判断を待つ
4. 承認を得てから代替実装に進む

ユーザー承認なしに代替判断は禁止。

---

## 3. 完了条件（厳密）

**セクション完了：すべて満たすこと**
- [ ] ビルドエラーなし
- [ ] Figma スクショ vs Local スクショの視覚比較で差異ゼロ または pixelmatch 2% 以下
- [ ] セマンティックHTMLセルフチェック済み（開発担当）
- [ ] アニメーション・ホバー・インタラクション動作確認済み
- [ ] review.md の該当差異が `[x]` または `[?]`（ユーザー承認待ち）
- [ ] ユーザー最終承認取得済み

**サイクル完了：すべて満たすこと**
- [ ] 全セクション完了
- [ ] improvement.md の全項目が「採用/保留/棄却」に分類済み
- [ ] 採用項目が CLAUDE.md に反映済み

---

## 4. コーディング規約

最頻違反の3点（詳細・全規約は `.claude/rules/coding.md`）：

- **BEMプレフィックス**：各mock_siteごとに固定（例：`sp-` for suisopot）
- **マジックナンバー禁止**：変数化必須
- **`!important` 禁止**（緊急時のみ、コメント必須）

---

## 5. ディレクトリ規則

- 画像: `public/mock_site/{ディレクトリ名}/images/`
- コンポーネント: `src/components/mock_site/{ディレクトリ名}/`
- スタイル: `src/styles/mock_site/{ディレクトリ名}/`
- ページ: `src/pages/mock_site/{ディレクトリ名}/`
- レイアウト: `src/layouts/mock_site/{ディレクトリ名}/`
- ユーザー手動エクスポート: `.claude/handoff/user-exports/`

---

## 6. ファイル参照ガイド

⚠️ 以下のタイミングで対応ファイルを**必ず**読むこと。読まずに作業開始禁止。

| タイミング | 読むファイル |
|---|---|
| サイクル開始時・Phase 0実施時 | `.claude/rules/workflow.md` |
| サブエージェント起動時 | `.claude/rules/roles.md` |
| 画像・アセット処理時 | `.claude/rules/assets.md` |
| 視覚比較・QA・エスカレーション時 | `.claude/rules/quality.md` |
| コード実装前（必須） | `.claude/rules/coding.md` |
