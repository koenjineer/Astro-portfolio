# 進捗

完了済みの作業の経緯と判断は `progress-log.md`（古い順）。2026-09-10〜11の分は `progress-log-archive.md`。
ここには最新の記録と次のアクションだけを置く。

## 2026-09-12 ホーム（ブランチ `feat/minami-home`）

やったこと：

- `/` を実装：メインビジュアル（写真3枚のスライダー＋お知らせ1件＋診療時間表）→ CONCEPT →
  当院の3つのおすすめ → 診療案内カード2枚 → スタッフブログ6件。疎通確認用の仮ページを丸ごと置き換えた。
  ページ内の部品は `app/_components/`
- 完成済みの2ページから共通部品を抜き出した（`components/layout/`）。抜き出しの前後で
  `/about/` `/medical/` `/staff/` `/news/` のHTMLが**完全一致**することを確かめてから先へ進んだ
  - `MediaBlock`（写真＋文章のブロックと水色の飾り。当院についてのポリシーと特徴とホームのCONCEPT）
  - `WaveBand`（上下の波＋水色の帯。診療案内の帯とホームの診療案内）
  - `MedicalTimeCard`（診療時間表の白いカード。フッターとホーム）
- データはWordPressから `getNewsPosts()`・`getBlogPosts()` の1回だけ。お知らせは先頭1件、ブログは先頭6件
- 素材9点を `public/images/home/` へ（`concept.webp` 640×438、`medical-general.webp` 400×267、
  `medical-special.webp` 460×290 は `cwebp -q 90`。おすすめのアイコン3点・飾り文字3点はSVG）
- Figmaの誤字・表記ゆれを直した：「貢献しきたい」→「貢献していきたい」（ユーザー決定）、
  「大事することで」→「大事にすることで」、「リニューアルを期に」→「機に」（当院についてのページと同じ直し）。
  「夜20:30まで営業」は診療時間表の22:00と食い違うが**Figmaのまま**（ユーザー決定）
- `lint`・`build` が通り、書き出しに `minami-dental-cms.local` が残らないこと、
  すべての `<img>` に width/height/alt/loading があること、見出しがh1→h2→h3の順であることを確認

判断したこと：

- **スライダーは自前のクライアント部品**（ライブラリを足さない）。5秒の自動送り＋左右の矢印＋ドット。
  動きを減らす設定では自動送りを止め、矢印・ドットに触れている間も止める（操作の直前に写真が変わるのを防ぐ）。
  一時停止ボタンは付けない（スタッフ紹介の写真の帯と同じ判断）。
  写真は飾り（`alt=""`）なので、切り替わりを読み上げる `aria-live` は付けない
- `"use client"` は `MainVisualSlider.tsx` の1つだけ。ページ本体はサーバーのままWordPressから取得する
- **ブログカードはホーム専用の別部品**にした。SPは一覧の `ArchiveCard` と寸法が完全に同じだが、
  PCは縦組み（札が写真に重なる・白いカードと影・3行で省略）で構造が変わり、
  `ArchiveCard` の `variant`（横組み1つの形で寸法だけ差し替える作り）では書けないため。
  既存8ルートへの影響も避けられる。`CategoryTag` とホバーの書き方は共有している
- **診療案内カード2枚は固定文言**。Figmaの「一般診療＝虫歯・入れ歯・小児歯科」はWordPressの診療タイプと
  噛み合わない（入れ歯はWPでは special、虫歯・インプラント・審美歯科はWPに無い）。リンク先は両方 `/medical/`（ユーザー決定）
- 診療時間表はフッターと同じ `footer-medical-time.webp` を流用。Figmaの表示サイズがPC477px・SP335pxで
  フッターとまったく同じだったため、書き出し直さない
- 診療案内カードの写真は元が400×267・460×290しかないので、その実寸のまま（PC表示460×290に対し1枚は少し引き伸ばし）
- Figmaに無いタブレット幅（768〜1023px）：おすすめは1列のまま（3列は1000px必要で青い札が折り返す）、
  診療案内カードは768pxから2列（1列だと728pxまで横に伸びる）、ブログはSPの横組みのまま（一覧ページと見え方がそろう）
- スライダーは3つに分けた：切り替えの仕組みと操作（`MainVisualSlider.tsx`）/ 写真の一覧というデータ（`homeSlides.ts`）/
  キャッチコピーの見た目（`CatchCopy.tsx`）。写真を差し替えるときも文言を直すときも、触る場所が1か所に決まるため
- ホームに `metadata` は書かない。`app/layout.tsx` の `title: SITE_NAME`（＝みなみ歯科）がそのまま正解のため

## 次のアクション

1. **全ページの通し確認**：これで7ページすべてが実装済み。実機のChromeで頭から見て、
   ヘッダー・フッター・固定ボタンの重なりや、ページ間のリンク切れがないかを確かめる
2. **WordPressの原稿の直し（ユーザー作業・任意）**：ブログの詳細はFigmaだと最後の見出しが「定期検診を受診しましょう」で
   「WEB予約」がリンクだが、WordPressは「まとめ」でリンク無し。blog-10・11 の日付もFigmaと違う（`docs/wordpress.md`）。
   **お知らせの本文の誤字**：「させていただきいます」が15件すべてに残っている。管理画面で直す（コードでは書き換えない）
3. 実機のChromeで開き、タブにfaviconが出ていることを確かめる（素材の書き出しの確認の残り）
4. Vercelプロジェクト `minami-dental-next` はまだ未作成。公開するなら作成 → `NEXT_PUBLIC_SITE_URL` を足して手元ビルド
