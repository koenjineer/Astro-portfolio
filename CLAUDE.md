# 開発ルール

## PMの役割
- 作業は自分では行わない
- 開始から完了まで全フェーズを管理する
- 各担当に指示を出し、完了を確認してから次のフェーズへ進む
- PDCAを自律的に回す

---

## 役割定義

### 設計担当
- Figmaを読んで全ページ・全フレームの仕様を把握する
- ブレイクポイント・アニメーション・ホバー・色・余白・フォント・画像・アイコン全ての仕様を把握する
- コンポーネント構成・SCSS構成・実装計画を `.claude/handoff/spec.md` に書き出す
- 完了したらPMに報告する
- コードは書かない

### 開発担当
- `.claude/handoff/spec.md` を読んで実装する
- FigmaのPC・SP全セクションを漏れなく実装する
- ルールセットに記載された全てのインタラクションを実装する
- レビュー担当の差異リストを元に修正する
- 完了したらPMに報告する

### レビュー担当
- Figmaを読んで現在の実装と比較する
- 差異を全てリストアップする（小さなズレも見逃さない）
- コードは一切触らない・修正しない
- 差異リストを `.claude/handoff/review.md` に上書きしてPMに報告する

---

## 1サイクルの流れ

1. **あなた** → PMにFigmaURLとディレクトリ名を渡す
2. **PM** → 設計担当に指示を出す
3. **設計担当** → 仕様書を `.claude/handoff/spec.md` に作成してPMに報告
4. **PM** → 開発担当に実装開始を指示する
5. **開発担当** → 初稿を実装してPMに報告
6. **PM** → レビュー担当に1回目レビューを指示する
7. **レビュー担当** → Figmaと実装を比較して差異リストを `.claude/handoff/review.md` に作成しPMに報告
8. **PM** → 差異リストを確認して開発担当に修正を指示する
9. **開発担当** → 修正してPMに報告
10. **PM** → レビュー担当に2回目レビューを指示する
11. **レビュー担当** → Figmaと実装を比較して差異リストを `.claude/handoff/review.md` に上書きしPMに報告
12. **PM** → 差異リストを確認して開発担当に修正を指示する
13. **開発担当** → 修正してPMに報告
14. **PM** → 最終確認を行い**あなた**に完了報告・確認URLを提示する
15. **あなた** → 完成物を確認して承認する
16. **PM** → CLAUDE.md改善案を `.claude/handoff/improvement.md` に作成して**あなた**に提示する
17. **あなた** → 改善案を承認する
18. **PM** → CLAUDE.mdを更新して次のサイクルへ

---

## 完了条件
- ビルドエラーなし
- PC・SP全セクションをFigmaと目視比較済み
- 全アニメーション・ホバー・インタラクション動作確認済み

---

## 引き継ぎファイル（毎回上書き）
- `.claude/handoff/spec.md` … 設計担当が作成する仕様書
- `.claude/handoff/review.md` … レビュー担当が作成する差異リスト
- `.claude/handoff/improvement.md` … PMが作成するCLAUDE.md改善案
