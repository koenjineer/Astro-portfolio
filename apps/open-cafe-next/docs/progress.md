# 進捗

## 2026-09-15 土台づくり（ブランチ `feat/open-cafe-scaffold`）

`apps/minami-dental-next` の設定をそのまま複製し（Next 16.3.3 / React 19.2.8 / Tailwind v4）、
データ取得の部品と疎通確認用の仮ページまで作った。ページ・ヘッダー・色・フォントはまだ無い。

- `lib/images.ts`：アイキャッチとACF画像の両方に使えるよう、型を `ImageConnection` にした。
  代わりの画像を持つ引数はやめ、画像が無ければ `null` を返す形にした（代わりの画像はまだ無いため）
- `lib/queries/`：news / menu / products / shops / top。WPの形を画面で使う形に変えて返し、`cache` で包む
- `app/page.tsx`：仮ページ。お知らせ15・カテゴリー4・メニュー29・ジャンル7・ギフト9・店舗3・ランチ4
- CIのmatrixに `open-cafe-next` を追加、`.claude/launch.json` にport 3002で追加

計画から変えたこと（実データを見て）：

- **お知らせ・メニューは、アイキャッチが無くてもビルドを止めない。** お知らせは5件（営業時間の4件＋post-03）、
  メニューはドリンク14品に最初から画像が無く、止めるとビルドが通らないため。`null` で返す
- お知らせは**カテゴリーが無いとビルドを止める**（カテゴリ別一覧に置けなくなるため）
- ギフトは価格が無いと止める。メニューの価格は全件 `null` なので止めない
- TOPはタイトル検索ではなく `page(id: "top", idType: URI)` で取る

## 2026-09-15 共通部分＋お問い合わせ（ブランチ `feat/open-cafe-contact`）

ルールセットの色・フォント、下層ページ上部（写真＋二重線＋メニューボタン）、ドロワーメニュー、パンくず、
フッター（吹き出し・地図画像・店舗情報・パンと豆の飾り・SNS）、ページトップへ戻る、デフォルトボタン、
お問い合わせ入力／完了を作った。仮ページ `app/page.tsx` は残し、共通フッターが載っても表示が崩れないことを確認。

計画から変えたこと・決めたこと：

- **フォントの太さはNoto Serif JP Medium(500)/Bold(700)**。計画は Regular/Bold だったが、ルールセットの指定がMedium/Bold
- **Damionは読み込むが先読みしない**（`preload: false`）。今回のページでは使っておらず、先読みすると全ページで読み込まれるため
- **色の名前**：Figmaの cr-base(#F3EFEB) は `base-light`、cr-base-dark は `base-dark`。`--color-base` は文字サイズの `text-base` と衝突するため（`/rules/tailwind.md`）
- **SNSアイコンはリンクにしない**。架空のカフェでリンク先が無いので、global-standard-next と同じく飾り（`aria-hidden`）として置いた。南歯科にはSNSが無い
- **吹き出しは画像だがaltを付けた**（「吉祥寺駅から徒歩5分！」という情報を持つため）。SVGはPC・SPで同じ形なので1枚にした
- **地図は1倍で書き出した**。元の画像が669×367しかなく、1.5倍にすると引き伸ばしになるため。文字の絵なので可逆webp
- **お問い合わせ項目のラジオは先頭「お店のご利用・ご予約について」を選んだ状態で始める**（Figmaどおり。ユーザー決定）。`required` は残す
- **メニューボタンは画面の右上に固定**（ユーザー決定）。写真の上では Figma と同じ右上10pxに見え、スクロールしても押せる。
  外枠を `fixed` にすると中のドロワーがその重なり順に閉じ込められるので、外枠を z-50（ページトップへ戻るの z-40 より上）にした
- **フォームの送信は `<form action={関数}>`**。`onSubmit` + `preventDefault` だと、JS起動前に押すと標準のGET送信で入力内容がURLに出るため（`/rules/nextjs-static-export.md`）
- **行の高さは文字サイズと一緒に書く**（`text-xs/[1.5]`）。親の `leading-[1.5]` は子の `text-xs` 等に打ち消されていた（`/rules/tailwind.md`）。見出しの日本語・ドロワーの項目・コピーライトを直した
- **読み上げの区切り**：見出しとドロワーの項目は、英字と日本語の間に見えない読点（`sr-only`）を入れ、「access、アクセス」「top、トップ」と読ませる
- **ハイコントラスト表示**：ラジオの選択の点は `forced-colors:bg-[CanvasText]` と `forced-color-adjust-none` で消えないようにした
- **ページトップへ戻るは画面に追従**させた（Figmaはフッター右下に固定）。南歯科と同じ判断。写真(320px)を過ぎたら出す
- **ドロワーの開閉は右へのスライド300ms**。Figmaに動きの指定は無い。動きを減らす設定では付けない
- **ドロワーは `<dialog>` を使わず、ドロワー以外を inert にする**。写真の中（本文側）に置いたので、ドロワーから body までの各階層の兄弟に付ける（`components/layout/drawerMenuInert.ts`）
- **開くときに visibility を遅らせない**。遅らせると開いた直後の1フレームは hidden 扱いで、閉じるボタンへのフォーカスが効かなかった（実機で発見）
- **`<html>` に `scrollbar-gutter: stable`**。ドロワーを開くとスクロールを止めてスクロールバーが消え、裏のページが約7px横にずれたため
- **フッターの地図の縦横比は白枠を含む箱で持つ**。width/height属性だけだと中身に比率がかかり、枠の分だけ9px背が高くなった
- **フッターの店舗情報のPC2列は `grid-rows-[repeat(3,auto)]`**。`grid-rows-3` だと行の高さがそろって45px高くなった

### タブレット幅（768〜1023px）の実測

PCの組みに切り替える幅を **`md`（768px）** にした。実機Chromeで768px（clientWidth 768）を測った値：

- 本文の枠は688px（768−左右20px×2＝728pxに収まる）。案内文は1行688pxで、PCと同じ長さ
- フォームはラベル列170px＋入力欄490px。PCと同じ寸法で、横はみ出し無し（scrollWidth 768）
- フッターのパン（右上の飾り）と「ACCESS」の文字の間は **6px** 空く（1280pxでは約260px）。重ならないので、パンはPCの大きさのまま
- 吹き出しは左端から80px、地図の上端（1902px）はパンの下端（1873px）より下
- SPの組みのまま768pxに伸ばすと、入力欄が728px幅まで伸びて1行が長くなるため採らなかった

**2026-09-16 追記：フッター左下の豆が店舗情報の文字に重なっていた**（768pxで TEL・Mail が読めなかった）。

- 原因：768px以上で豆をPCの大きさ（左−66px・幅322px・高さ289px、フッター上端から705px）で置いていた。
  本文の左端は「(画面幅−688)/2」なので、豆の右端256pxより本文が右に来る**画面幅約1200px以上**でないと、
  豆が店舗情報の左列（フッター上端から636〜778px）にかかる
- 判断：**768〜1279pxだけ豆を幅231px（高さ207px）・左−47pxに縮める**（はみ出し量も幅と同じ比率）。
  上端がフッター上端から787pxになり、店舗情報の最後の下線（778px）より下に来るので、本文の位置に関係なく文字にかからない。
  文字より奥に置く案は、豆も文字も濃い茶色で読めなくなるので採らなかった。1280px以上（`xl`）と375pxはFigmaのまま
- 実測（実機Chrome、同一オリジンのiframeで中身の幅を合わせ、画像の読み込み完了を確かめてから測った）：

| 中身の幅 | 豆（左・幅） | 豆と文字の重なり | 豆の上端と表の最後の文字の間 | パンと「ACCESS」の文字の間 |
|---|---|---|---|---|
| 375 | −29・121（Figma） | なし | 26px | −16px（Figmaと同じ位置で角がかかる） |
| 768 | −47・231 | なし | 16px | 6px |
| 820 / 900 / 1000 | −47・231 | なし | 16px | 32 / 72 / 122px |
| 1024 / 1100 / 1200 | −47・231 | なし | 16px | 134 / 172 / 222px |
| 1279 | −66・322（Figma） | なし | ―（本文の左端295pxが豆の右端より右） | 261px |
| 1280 / 1455 | −66・322（Figma） | なし | ― | 262 / 349px |

- 1279pxでFigmaの大きさになるのは、`xl`（1280px）の判定がスクロールバーを含む画面幅で行われるため。
  このときも本文の左端（295px）が豆の右端（256px）より右なので重ならない
- パンは768px以上で「ACCESS／アクセス」の文字にかからない（768pxで6px、幅が広いほど離れる）。
  メニューボタン（画面右上に固定）はスクロールするとどの幅でもパンの上に重なるが、ボタンが一番上にあり押せる

- **フッターの店舗情報の下余白は5px**（Figmaは6px）。Figmaの下線は内側の線で高さを増やさないが、CSSの border は1px足すため。
  6pxのままだと1行ごとに1px高くなり、フッターがPCで3px・SPで6px高かった

### Figmaとの差

レビュー指摘の修正後（行の高さ・店舗情報の下余白）に、書き出した `out/` を配信して測った値：

- フッターの高さ：PC 994px・SP 932px（Figmaと一致。修正前は 996px / 936px）
- 見出し「ACCESS／アクセス」：PC 99px（日本語21px）・SP 78px（日本語18px）。修正前は日本語が行の高さ1.33で縮んでいた
- 完了ページ全体の高さ：PC 1721px・SP 1431px（Figmaと一致）
- 残る差：1280pxではスクロールバーの幅（15px）を `scrollbar-gutter` で常に取っているので、中身の幅が1265pxになり、
  パンくず等が数px左に寄る（スクロールバーが重なって表示される環境では差は出ない）

確認方法：実機Chromeの拡張がつながらなかったため、手元の Google Chrome をヘッドレスで起動し、
DevTools Protocol で本物のマウス・キー入力を送って 1280px・375px を測った（JSを止めた状態の送信も同じ方法で確認）

## 2026-09-16 ギフト・贈り物（ブランチ `feat/open-cafe-products`）

`/products/` を作った。WPの `products` 9件を、1件目だけ大カード（PC 2列×2行、SP 1行）で並べ、下にラッピング案内。

計画から変えたこと・決めたこと：

- **並び順は投稿日時の古い順**（`DATE ASC`）。Figmaの並びと完全に一致するので、配列で持たない
- **「ショップで確認する」は押せるリンク**（ユーザー決定）。行き先が無いので `/products/` ＋ `scroll={false}`（押してもURL・スクロール位置が変わらないことを確認）。
  9個とも同じ文言なので `sr-only` で商品名を添えた
- **ボタンのホバーはルールセットのショップカードボタン（白地＋main色の文字）**。当初の計画の透明度70%は誤り。
  枠線1pxを常に付け、padding をFigmaから1px減らして高さをPC 37px / SP 30pxに保つ
- **ボタンをPCの寸法（幅174px）に切り替える幅は `lg`**（大・小カードとも）。768pxでは小カードが148pxしかなく、`md` で切り替えると文字が2行に折り返したため。
  768〜1023pxはSPの寸法（幅132px・高さ30px）で、`whitespace-nowrap` も付けた。
  実測：小カード 768px 148 / 900px 181 / 1023px 212 / 1024px 212（ボタン174）/ 1280px 245。375〜1280pxの6幅ともボタン1行・カード内・横スクロール無し
- **商品写真の alt は空**（計画は商品名）。WPから来る写真は飾り扱いの決まり（`/rules/headless-wordpress.md`）で、商品名は直下の h2 が伝え、alt に入れると読み上げで2回続くため
- **本文の下余白は160px**（計画はPC80/SP60だったが、Figmaの main は下160px。これで main の高さがFigmaと一致する）
- **ラッピング案内の枠の padding はFigmaから4px引いた値**（PC 56/75、SP 36）。Figmaの枠線は内側に描かれて寸法を増やさないため
- **点線のSVGは、書き出しに付く `preserveAspectRatio="none"` 等の外側の属性を外した**（図形は書き出しのまま）。
  付けたまま11px幅のタイルにすると点が横に伸びて楕円になるため。点は11pxタイルの中央に描かれるので、PCは位置を−4px、SPは `space` で左右に4pxはみ出させて端にそろえる
- **商品画像がpublic/に無いとビルドを止める**（`lib/images.server.ts` の `requireExistingLocalImageSrc`）。ギフトだけに効かせた。
  TOPのランチは画像がまだリポジトリに無く、仮ページ `app/page.tsx` が読むため。**TOPを作るときに同じ確認に切り替える**（店舗は切り替え済み）
- 商品写真はFigma「【ギフト】サムネイル画像」から1.5倍（792px）。シフォンケーキはレイヤー名が `img_item3` と誤っているので `img_item33.webp` にした。9枚ともWPの原本と見比べて一致
- ラッピングの写真は2倍（920×600、元1920×1280）。縦並びのタブレット幅で横888pxまで広がるため

### ラッピング案内を横並びにする幅：`lg`（1024px）

実測（ヘッドレスChrome、中身の幅はスクロールバー15pxを引いた値）：

| 画面幅 | 組み | 見出しの行数（1行の長さ） | 本文1行の最長 |
|---|---|---|---|
| 375 | 縦 | 2行（239px、折り返し任せ） | 252px |
| 768 | 縦 | 2行（429 / 288px） | 623px |
| 1023 | 縦 | 2行 | 766px |
| 1024 | 横（1列395px） | **3行**（383 / 46 / 288px。「す。」が1行に残る） | 383px |
| 1280 | 横（1列460px） | 2行（Figmaどおり） | 447px |

- `xl` にすると1024〜1279pxが縦並びになり、本文1行が766pxから最大1020pxまで伸びる。見出しの3行より読みにくいので `lg` にした
- 見出しが2行に収まるのは、1列が432px以上になる中身の幅約1085px以上
- 1024〜約1085pxでラッピング案内の見出しの「す。」だけが3行目に残るが、Figmaに無い幅なのでそのままにした（ユーザー決定）

### Figmaとの差（書き出した `out/` を配信して測った）

- 本文（写真・パンくずを除く）の高さ：PC 1967px・SP 2471px（Figmaと一致）。ページ全体もPC 3318px（Figmaと一致）
- カード幅：PC 大530・小245、SP 大335・小158（一致）。768pxは大336・小148、1024pxは大464・小212
- 横はみ出し無し（375 / 768 / 1024 / 1280 すべて scrollWidth = clientWidth）
- ラッピング案内：PC 1100×458、SP 335×511（一致）。点線は PC 42個・11px間隔、SPは23個（Figma 24個）

## 2026-09-16 店舗情報（ブランチ `feat/open-cafe-shop`）

`/shop/` を作った。WPの `shops` 3店舗を「見出し＋店内の写真＋店舗情報の表」で縦に並べる。
3ページ目なので、先に**見た目を変えない共通化**を済ませてからページを作った。

- `components/layout/SubPageLayout.tsx`：main＋上部の写真＋パンくず＋本文の余白（`px-5 pt-[60px] pb-40 md:pt-20`）。
  products と contact で完全に同じだった。本文の幅の上限（1100 / 688px）はページごとに違うので持たせない
- `components/shop/ShopInfoList.tsx`：住所・TELなどの表。フッターと寸法・並び・順序が一致するので共有する

決めたこと：

- **並び順は `lib/site.ts` の `SHOP_TITLE_ORDER`（店名の配列）で固定**（ユーザー決定）。WPの登録日順も
  偶然Figmaと同じだが、管理画面で登録し直すと崩れるため。配列に無い店名は末尾に回す。
  **全角スペースまで完全一致させること**（ずれてもエラーにならず、並びだけが静かに崩れる）
- **フッターの店舗情報は `lib/site.ts` の直書きのまま**（ユーザー決定）。フッターは全ページに出るので、
  WPの取得失敗で全ページが落ちる形にしない。共有するのは見た目の部品だけ
- **店舗のアイキャッチは「店内の写真」**。ファイル名が `map_*` なのは登録時の名残で、中身は地図ではない
- **画像はWPの原本をそのまま置いた**（1032×566＝Figmaの1.5倍）。webpを書き出し直すと二重に圧縮されるため。
  WPがどの店にどの画像かを持っているので、写真の取り違えも起きない
- **`requireExistingLocalImageSrc` を店舗にも効かせた**（ギフトに続いて2つ目）
- 見出しの縦棒は6px。Figmaの線は寸法を増やさないので、左余白はFigmaの16pxから棒の6pxを引いた10pxにする

### Figmaとの差（書き出した `out/` を配信して実測）

- ページ全体：PC 3688px（一致）。SP 3111px（Figmaのフレームは3207pxだが、下96pxは飾りのはみ出し用の余白）
- 店舗ブロック1つ：PC 688×619 / SP 335×565、写真 PC 688×377 / SP 335×184、表 PC 142 / SP 294（すべて一致）
- **フッターは994 / 932のまま**（共通化で1pxも動いていない）。既存3ページの書き出しHTMLも、
  ビルドごとに変わる値（ビルドID・チャンク名）を除いて共通化の前後で完全一致
- タブレット幅：768 / 1024pxとも表は2列（324px×2）、見出しは1行、横はみ出し無し。
  フッターの豆の飾りは768pxで上端3481px・表の下端3472pxとぎりぎり重ならない（既存の対処がそのまま効いた）

## 未決事項（Figma・原稿を見て決める）

- **TOPのドロワーボタン（19742:13912）**：TOPはファーストビューを過ぎたらドロワーボタンを出す指示。TOP実装時に作る
- **ギャラリー（Instagram）の実装方法**：TOPページ着手時に決める（ユーザー決定）
- **1ページの件数・ページ送りの有無**：お知らせ一覧・カテゴリ別、メニューのジャンル別
- **並び順**：ジャンル・お知らせのカテゴリーは仮にTERM_ID順。メニューは仮にslug昇順だが、
  slugが日本語なので意味のある順ではない（店舗と同じく、Figmaの並びを配列で持つことになりそう）
- **アイキャッチの無いお知らせ（post-02・03・06・11・15）**：代わりの画像を置くか、画像なしの見た目か
- **メニューの価格が全件 `null`**：未入力か、価格を出さない仕様か
- **お知らせ本文に埋め込まれたWP画像**（全15件の img_firstview_concept）：詳細ページで
  リポジトリの画像へ差し替えるか、管理画面で本文から外すか
- **TOPのランチ画像の置き場所**：メニューの画像と共有するか（今は `/images/home/` を指す）
